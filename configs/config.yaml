# config.yaml
defaults:
  - optimizer: adam
  - trainer: default

# Model-specific configuration
model:
  num_classes: 1000

dataset:
  name: Pokemon
  path: data/processed
  batch_size: 64

training:
  epochs: 20
  seed: 42

evaluation:
  model_checkpoint: checkpoints/best_model.pth
